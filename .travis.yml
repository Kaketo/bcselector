language: python

python:
  - "3.7"

before_install:
- python --version
- pip install -U pip
- pip install codecov

install:
- pip install -r requirements.txt

script:
- python run_tests.py

after_success:
  - codecov # submit coverage